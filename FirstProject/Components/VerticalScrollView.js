import React, {Conponent} from 'react';
import {AppRegistry,ScrollView, View, Text, TextInput, Dimension} from 'react-native';

export default class VerticalScrollView extends Component{
  render(){
    return(
      <ScrollView>
        <Image>


        </Image>
      </ScrollView>

    );

  }
}
